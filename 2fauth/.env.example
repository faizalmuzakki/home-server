# Domain for Traefik routing
DOMAIN=yourdomain.com

# App key - generate with: php artisan key:generate --show
# Or use: openssl rand -base64 32 (prefix with 'base64:')
APP_KEY=base64:YOUR_GENERATED_KEY_HERE
