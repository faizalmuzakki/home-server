services:
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin
    restart: unless-stopped
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-Asia/Jakarta}
    volumes:
      - ./jellyfin/config:/config
      - ./jellyfin/cache:/cache
      - ${MEDIA_PATH:-/data/media}:/media
    # LAN-only binding — Traefik handles all public access.
    ports:
      - "127.0.0.1:8096:8096"
    networks:
      - traefik-public
      - media-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.jellyfin.rule=Host(`jellyfin.${DOMAIN}`)"
      - "traefik.http.routers.jellyfin.entrypoints=websecure"
      - "traefik.http.routers.jellyfin.tls.certresolver=cloudflare"
      - "traefik.http.routers.jellyfin.middlewares=secure-defaults@file"
      - "traefik.http.services.jellyfin.loadbalancer.server.port=8096"
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M

  # -----------------------------------------------------------------------
  # Acquisition stack (Sonarr/Radarr/Prowlarr/qBittorrent/Bazarr) is disabled.
  # To revive: add a VPN gateway container (e.g. gluetun) and route
  # qbittorrent through it via `network_mode: "service:gluetun"`, then
  # uncomment the services below. Without a VPN, torrent traffic egresses
  # on your real residential IP, which is what we're avoiding here.
  # -----------------------------------------------------------------------
  # sonarr:
  #   image: linuxserver/sonarr:latest
  #   container_name: sonarr
  #   restart: unless-stopped
  #   environment:
  #     - PUID=${PUID:-1000}
  #     - PGID=${PGID:-1000}
  #     - TZ=${TZ:-Asia/Jakarta}
  #   volumes:
  #     - ./sonarr:/config
  #     - ${MEDIA_PATH:-/data/media}:/media
  #     - ${DOWNLOADS_PATH:-/data/downloads}:/downloads
  #   ports:
  #     - "127.0.0.1:8989:8989"
  #   networks:
  #     - traefik-public
  #     - media-network
  #   labels:
  #     - "traefik.enable=true"
  #     - "traefik.http.routers.sonarr.rule=Host(`sonarr.${DOMAIN}`)"
  #     - "traefik.http.routers.sonarr.entrypoints=websecure"
  #     - "traefik.http.routers.sonarr.tls.certresolver=cloudflare"
  #     - "traefik.http.routers.sonarr.middlewares=admin-secure@file"
  #     - "traefik.http.services.sonarr.loadbalancer.server.port=8989"
  #
  # radarr:
  #   image: linuxserver/radarr:latest
  #   container_name: radarr
  #   restart: unless-stopped
  #   environment:
  #     - PUID=${PUID:-1000}
  #     - PGID=${PGID:-1000}
  #     - TZ=${TZ:-Asia/Jakarta}
  #   volumes:
  #     - ./radarr:/config
  #     - ${MEDIA_PATH:-/data/media}:/media
  #     - ${DOWNLOADS_PATH:-/data/downloads}:/downloads
  #   ports:
  #     - "127.0.0.1:7878:7878"
  #   networks:
  #     - traefik-public
  #     - media-network
  #   labels:
  #     - "traefik.enable=true"
  #     - "traefik.http.routers.radarr.rule=Host(`radarr.${DOMAIN}`)"
  #     - "traefik.http.routers.radarr.entrypoints=websecure"
  #     - "traefik.http.routers.radarr.tls.certresolver=cloudflare"
  #     - "traefik.http.routers.radarr.middlewares=admin-secure@file"
  #     - "traefik.http.services.radarr.loadbalancer.server.port=7878"
  #
  # prowlarr:
  #   image: linuxserver/prowlarr:latest
  #   container_name: prowlarr
  #   restart: unless-stopped
  #   environment:
  #     - PUID=${PUID:-1000}
  #     - PGID=${PGID:-1000}
  #     - TZ=${TZ:-Asia/Jakarta}
  #   volumes:
  #     - ./prowlarr:/config
  #   ports:
  #     - "127.0.0.1:9696:9696"
  #   networks:
  #     - traefik-public
  #     - media-network
  #   labels:
  #     - "traefik.enable=true"
  #     - "traefik.http.routers.prowlarr.rule=Host(`prowlarr.${DOMAIN}`)"
  #     - "traefik.http.routers.prowlarr.entrypoints=websecure"
  #     - "traefik.http.routers.prowlarr.tls.certresolver=cloudflare"
  #     - "traefik.http.routers.prowlarr.middlewares=admin-secure@file"
  #     - "traefik.http.services.prowlarr.loadbalancer.server.port=9696"
  #
  # qbittorrent:
  #   # MUST route through a VPN container — do not enable this without one.
  #   image: linuxserver/qbittorrent:latest
  #   container_name: qbittorrent
  #   restart: unless-stopped
  #   environment:
  #     - PUID=${PUID:-1000}
  #     - PGID=${PGID:-1000}
  #     - TZ=${TZ:-Asia/Jakarta}
  #     - WEBUI_PORT=8081
  #   volumes:
  #     - ./qbittorrent:/config
  #     - ${DOWNLOADS_PATH:-/data/downloads}:/downloads
  #   # network_mode: "service:gluetun"  # uncomment once gluetun exists
  #   ports:
  #     - "127.0.0.1:8081:8081"
  #     - "6881:6881"
  #     - "6881:6881/udp"
  #   networks:
  #     - traefik-public
  #     - media-network
  #
  # bazarr:
  #   image: linuxserver/bazarr:latest
  #   container_name: bazarr
  #   restart: unless-stopped
  #   environment:
  #     - PUID=${PUID:-1000}
  #     - PGID=${PGID:-1000}
  #     - TZ=${TZ:-Asia/Jakarta}
  #   volumes:
  #     - ./bazarr:/config
  #     - ${MEDIA_PATH:-/data/media}:/media
  #   ports:
  #     - "127.0.0.1:6767:6767"
  #   networks:
  #     - traefik-public
  #     - media-network
  #   labels:
  #     - "traefik.enable=true"
  #     - "traefik.http.routers.bazarr.rule=Host(`bazarr.${DOMAIN}`)"
  #     - "traefik.http.routers.bazarr.entrypoints=websecure"
  #     - "traefik.http.routers.bazarr.tls.certresolver=cloudflare"
  #     - "traefik.http.routers.bazarr.middlewares=admin-secure@file"
  #     - "traefik.http.services.bazarr.loadbalancer.server.port=6767"

networks:
  traefik-public:
    external: true
  media-network:
    name: media-network
    driver: bridge
