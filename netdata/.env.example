# Domain for Traefik routing
DOMAIN=solork.dev

# Basic auth for Netdata (generate with: htpasswd -nb username password)
# Example: admin:$apr1$xxxxx...
NETDATA_AUTH=admin:$$apr1$$placeholder

# Optional: Netdata Cloud claim token (for cloud dashboard)
# Get from: https://app.netdata.cloud/
NETDATA_CLAIM_TOKEN=
NETDATA_CLAIM_URL=https://app.netdata.cloud
