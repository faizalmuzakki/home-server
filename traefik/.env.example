# Your domain
DOMAIN=example.com

# Cloudflare credentials for DNS challenge (SSL certs)
CF_API_EMAIL=your-email@example.com
CF_DNS_API_TOKEN=your-cloudflare-dns-api-token

# Traefik dashboard auth (generate with: htpasswd -nb admin yourpassword)
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$xyz...
